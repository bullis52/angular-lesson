import { BoldAndColorDirective } from './bold-and-color.directive';

describe('BoldAndColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldAndColorDirective();
    expect(directive).toBeTruthy();
  });
});
